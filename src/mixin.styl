siteHeaderHeight = 48px

borderColor = #ddd
mainColor = #16B4E1

inputHeight = 32px
inputFontSize = 13px

hh(height)
    height height
    line-height height

fixed(top = 0, right = 0, bottom = 0, left = 0)
    position fixed
    top top
    left left
    bottom bottom
    right right

absolute(top = 0, right = 0, bottom = 0, left = 0)
    position absolute
    top top
    left left
    bottom bottom
    right right

textOverflow()
    text-overflow ellipsis
    word-break break-all
    white-space nowrap
    overflow hidden

